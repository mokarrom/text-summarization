[pytest]
pythonpath = "src"

minversion = 5.0

testpaths = tests

junit_family = xunit2
junit_logging = all

log_cli = 1
log_cli_level = INFO

log_auto_indent = True
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

addopts =
    -k='not test_container'